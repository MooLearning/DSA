```

✍️ Author            :    Mostofa Morshed Sayeem
🌐 Profile           :    https://github.com/smmmsmo

🌐 Credits/Links     :    

```

# 📃 Rotate Players Right

---

## ❓ Question / Description

A game lobby stores player IDs in an array. Before the match starts, the last player becomes the first player, and everyone else shifts one step right.

Method Name: rotatePlayersRight

| Input | Output |
|:-------------|:--------------|
| `players = [10, 20, 30, 40, 50]` | `[50, 10, 20, 30, 40]` |

---

## 🎀 C Code 🎀

```c

REMOVEME

```

---

## 🎀 C++ Code 🎀

```C++

REMOVEME

```

---

## 🎀 Java Code 🎀

```java

import java.io.FileInputStream;
import java.io.PrintStream;
import java.util.Arrays;
import java.util.Scanner;

public class Q9 {
  public static void rotatePlayersRight(int[] players) {
    if (players.length == 0) {
      return;
    }
    int lastPlayer = players[players.length - 1];
    for (int i = players.length - 1; i > 0; i--) {
      players[i] = players[i - 1];
    }
    players[0] = lastPlayer;
    System.out.println(Arrays.toString(players));
  }

  public static void main(String[] args) throws Exception {
    if (System.getProperty("ONLINE_JUDGE") == null) {
      System.setIn(new FileInputStream("input.txt"));
      System.setOut(new PrintStream("output.txt"));
    }

    Scanner sc = new Scanner(System.in);

    int[] players = new int[5];
    for (int i = 0; i < players.length; i++) {
      players[i] = sc.nextInt();
    }
    rotatePlayersRight(players);
    sc.close();
  }
}

```

---

## 🎀 Python Code 🎀

```Python

REMOVEME

```

---
